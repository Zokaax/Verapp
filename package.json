{
  "name": "verapp",
  "module": "index.ts",
  "dependencies": {
    "browser-sync": "^3.0.4",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "private": true,
  "scripts": {
    "dev": "bun run app.js",
    "browser": "browser-sync start --proxy 'http://localhost:3000' --files '**/*.html, **/*.ejs, **/*.css, **/*.js'"
  },
  "type": "module"
}
